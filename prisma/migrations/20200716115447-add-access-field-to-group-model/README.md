# Migration `20200716115447-add-access-field-to-group-model`

This migration has been generated by André 'Dezzy' at 7/16/2020, 11:54:47 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "GroupAccess" AS ENUM ('ANY', 'MUNICIPAL_SPHERE', 'STATE_SPHERE', 'CITIES');

ALTER TABLE "public"."groups" ADD COLUMN "access" "GroupAccess" NOT NULL DEFAULT E'ANY';
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200714125855-create-groups..20200716115447-add-access-field-to-group-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -532,9 +532,17 @@
 model Group {
   @@map(name: "groups")
-  id     String @default(uuid()) @id
+  id     String      @default(uuid()) @id
   name   String
+  access GroupAccess @default(ANY)
   cities City[]
   users  User[]
 }
+
+enum GroupAccess {
+  ANY
+  MUNICIPAL_SPHERE
+  STATE_SPHERE
+  CITIES
+}
```


