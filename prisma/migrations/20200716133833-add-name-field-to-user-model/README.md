# Migration `20200716133833-add-name-field-to-user-model`

This migration has been generated by André 'Dezzy' at 7/16/2020, 1:38:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."users" ADD COLUMN "name" text  NOT NULL ;

CREATE UNIQUE INDEX "users.username" ON "public"."users"("username")

CREATE UNIQUE INDEX "users.email" ON "public"."users"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200716115447-add-access-field-to-group-model..20200716133833-add-name-field-to-user-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -522,10 +522,11 @@
 model User {
   @@map(name: "users")
   id       String  @default(uuid()) @id
-  username String
-  email    String
+  name     String
+  username String  @unique
+  email    String  @unique
   active   Boolean @default(true)
   group    Group   @relation(fields: [groupId], references: [id])
   groupId  String
 }
```


