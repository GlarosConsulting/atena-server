# Migration `20200717093744-add-company-field-to-agreement-model`

This migration has been generated by André 'Dezzy' at 7/17/2020, 9:37:44 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."agreements" DROP COLUMN "city",
DROP COLUMN "uf",
ADD COLUMN "companyId" text   ;

ALTER TABLE "public"."agreements" ADD FOREIGN KEY ("companyId")REFERENCES "public"."companies"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200717085126-alter-group-field-set-optional..20200717093744-add-company-field-to-agreement-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,22 +2,74 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
+model City {
+  @@map(name: "cities")
+
+  id        String    @default(uuid()) @id
+  name      String
+  uf        String
+  ibge      String    @unique
+  companies Company[]
+  groups    Group[]
+}
+
+model Company {
+  @@map(name: "companies")
+
+  id        String      @default(uuid()) @id
+  cnpj      String      @unique
+  name      String
+  city      City        @relation(fields: [cityId], references: [id])
+  cityId    String
+  sphere    String
+  Agreement Agreement[]
+}
+
+model User {
+  @@map(name: "users")
+
+  id       String  @default(uuid()) @id
+  name     String
+  username String  @unique
+  email    String  @unique
+  active   Boolean @default(true)
+  group    Group?  @relation(fields: [groupId], references: [id])
+  groupId  String?
+}
+
+model Group {
+  @@map(name: "groups")
+
+  id     String      @default(uuid()) @id
+  name   String
+  access GroupAccess @default(ANY)
+  cities City[]
+  users  User[]
+}
+
+enum GroupAccess {
+  ANY
+  MUNICIPAL_SPHERE
+  STATE_SPHERE
+  CITIES
+}
+
 model Agreement {
   @@map(name: "agreements")
   id                  String               @default(uuid()) @id
   agreementId         String?
-  uf                  String?
-  city                String?
+  company             Company?             @relation(fields: [companyId], references: [id])
+  companyId           String?
   name                String?
   status              String?
   start               DateTime?
   end                 DateTime?
@@ -495,55 +547,4 @@
   transferValue    Float?
   counterpartValue Float?
   yieldValue       Float?
 }
-
-model City {
-  @@map(name: "cities")
-
-  id        String    @default(uuid()) @id
-  name      String
-  uf        String
-  ibge      String    @unique
-  companies Company[]
-  groups    Group[]
-}
-
-model Company {
-  @@map(name: "companies")
-
-  id     String @default(uuid()) @id
-  cnpj   String @unique
-  name   String
-  city   City   @relation(fields: [cityId], references: [id])
-  cityId String
-  sphere String
-}
-
-model User {
-  @@map(name: "users")
-
-  id       String  @default(uuid()) @id
-  name     String
-  username String  @unique
-  email    String  @unique
-  active   Boolean @default(true)
-  group    Group?  @relation(fields: [groupId], references: [id])
-  groupId  String?
-}
-
-model Group {
-  @@map(name: "groups")
-
-  id     String      @default(uuid()) @id
-  name   String
-  access GroupAccess @default(ANY)
-  cities City[]
-  users  User[]
-}
-
-enum GroupAccess {
-  ANY
-  MUNICIPAL_SPHERE
-  STATE_SPHERE
-  CITIES
-}
```


